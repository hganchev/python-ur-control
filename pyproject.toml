[project]
name = "pyUr"
version = "1.0.0.0"
description = "A package for controling Universal Robots with Dashboard Commands and Realtime interface Commands"

[build-system]
requires = ["setuptools >= 61.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package.find]
where = ["src"]